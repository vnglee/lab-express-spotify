{{body.items.images}}

{{#each body.items}}
    <h1>{{name}}</h1>
       <p><img src={{images.0.url}} alt="artist image" width="200px"></p>
       <p><button class="links"><a href="/tracks/{{id}}">View Tracks</a></button></p>

{{/each}}